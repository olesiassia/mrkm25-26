# Лабораторна робота №3

## Тема: Реалізація протоколу незаперечного цифрового підпису

**Виконав:** Булигін Олексій  
**Дата:** 24 грудня 2025

---

## 1. Мета роботи

Реалізувати протокол незаперечного цифрового підпису (Undeniable Digital Signature) на основі бібліотеки Crypto++.

---

## 2. Теоретичні відомості

### 2.1 Незаперечний цифровий підпис

Незаперечний цифровий підпис — це криптографічний примітив, запропонований Девідом Чаумом та Гансом ван Антверпеном у 1989 році. На відміну від звичайного цифрового підпису, незаперечний підпис має такі властивості:

1. **Інтерактивна верифікація** — перевірка підпису вимагає участі підписанта
2. **Непередаваність** — верифікатор не може довести третій стороні, що підпис дійсний
3. **Можливість заперечення** — підписант може довести, що підпис є підробленим

### 2.2 Протокол Чаума-ван Антверпена

#### Параметри системи

- $p$ — безпечне просте число ($p = 2q + 1$, де $q$ також просте)
- $g$ — генератор підгрупи порядку $q$ в $\mathbb{Z}_p^*$
- $x$ — секретний ключ ($x \in [1, q-1]$)
- $y = g^x \mod p$ — відкритий ключ

#### Алгоритм підпису

Для повідомлення $m$:
1. Обчислити хеш повідомлення: $h = H(m)$
2. Відобразити на елемент групи: $M = g^{h \mod q} \mod p$
3. Обчислити підпис: $s = M^x \mod p$

#### Протокол верифікації

1. **Верифікатор** генерує випадкові $a, b \in [1, q-1]$
2. **Верифікатор** обчислює виклик: $C = M^a \cdot g^b \mod p$
3. **Підписант** обчислює відповідь: $R = C^x \mod p$
4. **Верифікатор** перевіряє: $R \stackrel{?}{=} s^a \cdot y^b \mod p$

Якщо рівність виконується, підпис вважається дійсним.

#### Протокол заперечення

Якщо підпис $s$ не є дійсним (тобто $s \neq M^x \mod p$), підписант може це довести:

1. Виконати кілька раундів верифікації з різними випадковими викликами
2. Якщо всі раунди послідовно завершуються невдачею, це доводить недійсність підпису

### 2.3 Математичне обґрунтування протоколу

#### Крок 1: Чому використовуються безпечні прості числа?

Безпечне просте число $p = 2q + 1$ гарантує, що група $\mathbb{Z}_p^*$ має підгрупу простого порядку $q$. Це важливо для безпеки:

- Порядок групи $\mathbb{Z}_p^* = p - 1 = 2q$
- Підгрупа квадратичних лишків має порядок $q$ (просте число)
- Задача дискретного логарифму в такій групі є обчислювально складною

#### Крок 2: Генератор підгрупи

Генератор $g$ обирається так, щоб $g^q \equiv 1 \pmod{p}$ і $g \neq 1$. Це гарантує:

$$\langle g \rangle = \{g^0, g^1, g^2, \ldots, g^{q-1}\} \subset \mathbb{Z}_p^*$$

Будь-який елемент $h \in \mathbb{Z}_p^*$ можна перетворити на елемент підгрупи: $g = h^2 \mod p$.

#### Крок 3: Створення підпису

Підпис $s = M^x \mod p$ створює зв'язок між повідомленням $M$ та секретним ключем $x$:

$$s = M^x \mod p$$

Це аналогічно схемі Діффі-Хеллмана: знаючи $M$ і $s$, неможливо обчислити $x$ без розв'язання задачі дискретного логарифму.

#### Крок 4: Математика протоколу верифікації

**Твердження:** Якщо $s = M^x \mod p$ (підпис дійсний), то $R = s^a \cdot y^b \mod p$.

**Доведення:**

Верифікатор надсилає виклик:
$$C = M^a \cdot g^b \mod p$$

Підписант обчислює відповідь:
$$R = C^x \mod p = (M^a \cdot g^b)^x \mod p = M^{ax} \cdot g^{bx} \mod p$$

Очікуване значення верифікатора:
$$s^a \cdot y^b \mod p = (M^x)^a \cdot (g^x)^b \mod p = M^{ax} \cdot g^{bx} \mod p$$

Отже:
$$R = s^a \cdot y^b \mod p \quad \checkmark$$

#### Крок 5: Чому підпис не можна верифікувати без підписанта?

Верифікатор знає: $M$, $s$, $g$, $y$, $p$

Щоб перевірити $s = M^x$ самостійно, потрібно перевірити:
$$\log_g(y) \stackrel{?}{=} \log_M(s)$$

Це еквівалентно розв'язанню задачі **Decisional Diffie-Hellman (DDH)**, яка є обчислювально складною.

#### Крок 6: Чому протокол є zero-knowledge?

Транскрипт $(C, R)$ не розкриває інформації про $x$:

1. $C$ — випадкове значення, обране верифікатором
2. $R = C^x$ — може бути згенеровано симулятором без знання $x$:
   - Симулятор обирає випадкове $R$
   - Обчислює $C = R^{x^{-1}}$ (якщо знає $x^{-1}$)
   
Оскільки транскрипт можна симулювати, він не містить інформації, яку можна передати третій стороні.

#### Крок 7: Протокол заперечення — математика

Якщо $s \neq M^x$ (підпис підроблений), то існує $\delta \neq 1$ таке, що:
$$s = M^x \cdot \delta \mod p$$

Тоді відповідь підписанта:
$$R = C^x = M^{ax} \cdot g^{bx} \mod p$$

Але очікуване значення верифікатора:
$$s^a \cdot y^b = (M^x \cdot \delta)^a \cdot g^{bx} = M^{ax} \cdot \delta^a \cdot g^{bx} \mod p$$

Оскільки $\delta^a \neq 1$ для випадкового $a$:
$$R \neq s^a \cdot y^b \mod p$$

Виконавши кілька раундів з різними $a$, можна статистично довести, що підпис недійсний.

---

## 3. Опис реалізації

### 3.1 Структура проекту

```
lab3_Bulygin_Oleksii/
├── undeniable_sig.h      # Заголовний файл з оголошеннями
├── undeniable_sig.cpp    # Реалізація протоколу
├── test.cpp              # Тестовий набір
├── CMakeLists.txt        # Конфігурація CMake
└── Makefile              # Makefile для збірки
```

### 3.2 Основні класи та структури

```cpp
class UndeniableSignature {
    struct PublicKey {
        Integer p;  // Просте число (модуль)
        Integer q;  // Порядок підгрупи
        Integer g;  // Генератор
        Integer y;  // Відкритий ключ: y = g^x mod p
    };
    
    struct PrivateKey {
        Integer p, q, g;
        Integer x;  // Секретний експонент
    };
    
    struct Signature {
        Integer s;  // Значення підпису: s = m^x mod p
    };
    
    struct VerificationChallenge {
        Integer C;  // Виклик: C = m^a * g^b mod p
        Integer a, b;  // Випадкові значення верифікатора
    };
    
    struct VerificationResponse {
        Integer R;  // Відповідь: R = C^x mod p
    };
};
```

### 3.3 Основні функції

| Функція | Опис |
|---------|------|
| `generateKeys()` | Генерація ключової пари (повільна, генерує нові прості числа) |
| `generateKeysFast()` | Швидка генерація ключів з використанням RFC 3526 |
| `sign()` | Створення підпису для повідомлення |
| `createChallenge()` | Створення виклику верифікатором |
| `respondToChallenge()` | Обчислення відповіді підписантом |
| `verifyResponse()` | Перевірка відповіді верифікатором |
| `interactiveVerify()` | Повний протокол верифікації |
| `denySignature()` | Протокол заперечення підпису |

### 3.4 Оптимізація генерації ключів (RFC 3526)

Генерація безпечних простих чисел є дуже повільною операцією. Для прискорення роботи реалізовано підтримку стандартних параметрів з **RFC 3526** (More Modular Exponential Diffie-Hellman groups).

| Розмір | Група RFC 3526 | Час генерації |
|--------|----------------|---------------|
| 1536 біт | Group 5 | < 1 мс |
| 2048 біт | Group 14 | < 1 мс |
| 3072 біт | Group 15 | < 1 мс |
| 4096 біт | Group 16 | < 1 мс |

### 3.5 Використані криптографічні примітиви

- **SHA-256** — хешування повідомлень
- **Безпечні прості числа** — $p = 2q + 1$
- **Модулярна арифметика** — операції в $\mathbb{Z}_p^*$
- **Криптографічно стійкий ГПВЧ** — `AutoSeededRandomPool`

---

## 4. Тестування

### 4.1 Перелік тестів

| № | Назва тесту | Опис |
|---|-------------|------|
| 1 | Basic Sign and Verify | Базовий сценарій підпису та верифікації |
| 2 | Invalid Signature Detection | Виявлення недійсних підписів |
| 3 | Denial Protocol | Протокол заперечення підпису |
| 4 | Multiple Messages | Підпис кількох повідомлень одним ключем |
| 5 | Serialization | Серіалізація та десеріалізація ключів |
| 6 | Protocol Transcript | Детальний протокол верифікації |
| 7 | Edge Cases | Граничні випадки (порожні, довгі повідомлення) |

### 4.2 Результати тестування

#### Тест 1: Базовий підпис та верифікація
- Створено підпис для тестового повідомлення
- Верифікація пройшла успішно через інтерактивний протокол

#### Тест 2: Виявлення недійсних підписів
- Перевірено підпис з неправильним повідомленням → відхилено
- Перевірено підроблений підпис → відхилено
- Перевірено підпис з неправильним відкритим ключем → відхилено

#### Тест 3: Протокол заперечення
- Дійсний підпис не може бути заперечений
- Недійсний підпис успішно заперечено (4 раунди)

#### Тест 4: Кілька повідомлень
- Підписано та верифіковано 5 різних повідомлень одним ключем
- Всі підписи пройшли верифікацію

#### Тест 5: Серіалізація
- Ключі та підпис збережено у файли
- Успішно завантажено та верифіковано

#### Тест 6: Протокол верифікації
- Продемонстровано два раунди з різними випадковими значеннями
- Обидва раунди пройшли успішно

#### Тест 7: Граничні випадки
- Коротке повідомлення ("Hi") → VALID
- Порожнє повідомлення ("") → VALID
- Довге повідомлення (4 КБ) → VALID
- Повідомлення з Unicode ("Тест! 日本語 🔐") → VALID

---

## 5. Висновки

1. Успішно реалізовано протокол незаперечного цифрового підпису Чаума-ван Антверпена на основі бібліотеки Crypto++.

2. Реалізація включає:
   - Генерацію безпечних простих чисел та ключів
   - Швидку генерацію ключів з використанням RFC 3526
   - Алгоритм підпису
   - Інтерактивний протокол верифікації
   - Багатораундовий протокол заперечення

3. Створено набір тестів для перевірки коректності імплементації

4. Продемонстровано властивості незаперечного підпису:
   - Інтерактивна верифікаціяя
   - Можливість заперечення підробленого підпису
   - Неможливість передачі доказу підпису

---


## Список використаних джерел

1. Chaum, D., van Antwerpen, H. (1989). "Undeniable Signatures". CRYPTO '89.
2. Crypto++ Library Documentation. https://www.cryptopp.com/
